
// Creates namespace
if (typeof window.PartyConnect === "undefined") {
    window.PartyConnect = {};
}

/**
* Sends data on server.
*
* @method sendData
* @author Jan Herzan
*/
PartyConnect.sendData = function () {
    var url = party_connect_plugin_params.ajaxurl;
};
